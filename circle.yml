dependencies:
  pre:
    - sudo apt-get install coinor-cbc coinor-clp
    - wget http://ftp.gnu.org/gnu/glpk/glpk-4.60.tar.gz
    - tar xvfz glpk-4.60.tar.gz
    - cd rams/glpk-4.60 && make && sudo make install

test:
  post:
    - LD_LIBRARY_PATH=/usr/local/lib bundle exec rake test
